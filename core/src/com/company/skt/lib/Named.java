package com.company.skt.lib;

/** An {@code interface} for different libGDX-derived classes to provide the ability to search for components by their
 * name without having a reference to them. <br>
*/
public interface Named {
    String getName();
}
